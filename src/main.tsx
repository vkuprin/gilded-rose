import React from "react";
import ReactDOM from "react-dom/client";
import GildedRoseComponent from "./components/GildedRose.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GildedRoseComponent />
  </React.StrictMode>,
);
